# Claude Warm-Up Scheduler

A Cloud Scheduled Task that runs weekdays at 4:55 AM Panama Time (GMT-5) to start the Claude token consumption window early.

Skips 11 fixed Panama public holidays. See `docs/` for the full design spec.
